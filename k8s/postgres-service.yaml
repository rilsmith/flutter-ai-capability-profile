apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: ns-team-ads-test
  labels:
    app: postgres
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
