apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: ns-team-ads-test
  labels:
    app: api
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
    - port: 5000
      targetPort: 5000
      name: http
