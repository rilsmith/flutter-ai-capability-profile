apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: ns-team-ads-test
  labels:
    app: web
spec:
  type: ClusterIP
  selector:
    app: web
  ports:
    - port: 80
      targetPort: 80
      name: http
